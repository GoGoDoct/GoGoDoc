# UI
streamlit==1.40.0

# PDF 파싱 및 렌더링
pdfplumber==0.11.4
pdf2image==1.17.0

# 수치 구조화 및 검증
pydantic==2.9.2

# 항목명 퍼지 매칭 fallback
rapidfuzz==3.10.1

# LLM
openai==1.57.0

# 벡터 RAG (retriever=pgvector 시) - 임베딩은 ::vector 캐스트로 전송
psycopg[binary]==3.2.3

# 환경 변수 관리
python-dotenv==1.0.1
